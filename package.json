{
  "name": "@vtex-tickets/common",
  "version": "1.0.0",
  "description": "Common utilities for the VTEX ticketing system",
  "main": "index.js",
  "types": "index.d.ts",
  "scripts": {
    "clean": "rm -rf ./build/**/*",
    "publish": "npm publish --access public",
    "build": "npm run clean && tsc"
  },
  "files": [
    "./build/**/*"
  ],
  "keywords": ["vtex", "tickets", "common", "utilities"],
  "author": "Your Name",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^4.0.0"
  }
}
